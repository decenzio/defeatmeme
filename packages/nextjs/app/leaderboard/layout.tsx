export const metadata = {
  title: "Leaderboard - DefeatMeme",
  description: "View the top scores and rankings in DefeatMeme planet defense game",
};

export default function LeaderboardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
